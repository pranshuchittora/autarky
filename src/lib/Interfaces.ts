export interface IPromptSelect {
  name: string;
  label: string;
  value: string;
  size: number;
  size_label: string;
  time_label: string;
}

export interface IRefinedListItem {
  path: string;
  age: any;
}
